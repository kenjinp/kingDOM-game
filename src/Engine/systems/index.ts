import render from "./render";
import keyDebugPopup from "./keyDebugPopup";

// Just a simple list of the systems to be enacted on in order
// via the game loop
export default [keyDebugPopup, render];
