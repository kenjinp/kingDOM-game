import {entities} from "./entities.yaml";

// To be decided how the entities manager works.
// Now we just load a debug yaml
const GAME_ENTITIES: any[] = entities

console.log(GAME_ENTITIES)
export default GAME_ENTITIES